apiVersion: v1
kind: Service
metadata:
  name: ai-forge-frontend
  namespace: ai-forge
  labels:
    app: ai-forge-frontend
spec:
  type: ClusterIP
  selector:
    app: ai-forge-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
